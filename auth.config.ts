export default {
  // same matcher idea
  matcher: [
    "/dashboard/:path*",
    "/organizations/:path*",
    "/attendance/:path*",
    "/employees/:path*",
    "/leaves/:path*",
    "/payroll/:path*",
    "/login",
    "/register",
  ]
}
